Changing file permission:

sudo chmod -R o+rw filename

-- Remove the history from 
rm -rf .git

remote:
git remote show origin
git remote remove origin

pushing:

git init 
git remote add origin gitclone
git add .
git commit -m "Name"


TAR.GZ:

tar -xzf archive.tar.gz
tar -xzf archive-name.tar.gz
cd archive-name
./configure
make
sudo make install


# project pushing 
git init
git checkout -b client-home <= (creating new branch -b is new branch)
git checkout client-home <=(move to branch)
git status <=(check changings)
git add .<=(adding changes (. means all))
git status <=(chack changings)
git commit -m "Create initial product page and link to router" <=((-m means message) " " relatively that message)
git push origin client-home <=(push zhiberu pull alu)
git status <=(chack changings)
git master <=()


git master <=(masterdy alu)
git branch master
git checkout master
git pull origin master

git checkout -b product-page


New project 
git init
git add .
git commit -m "sth"
git remote add origin URL
(Sets the new remote)
git remote -v
(Verifies the new remote URL)
git push -f origin master


npx husky install
npx lins-staged
